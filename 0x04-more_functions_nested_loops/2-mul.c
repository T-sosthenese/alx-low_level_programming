#include "main.h"
/**
 * mul - multiplies two integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
